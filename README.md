strctr — набор шаблонов, скриптов для проектирования интерфейсов и логики на стороне сервера (framework).

Синтаксис автора может и будет отличаться от общепринятых стандартов программирования среди сообществ разработчиков, этот проект создан для самовыражения и приобритения навыков программирования.

Работа с фреймворком на уровне http запросов отталкивается от понимания устройства каталогов в операционной системе, способов сопоставления с пространством имен и методов обращения к файлам средствами http. Архитектура скриптов отражает SOLID подход, но без строго соблюдения всех принципов лежащих в основе проектирования данным способом.

Требования и цели в текущей версии

1. Все url обращения направлены на index.php для разбора uri на компоненты: расширение файла, имени файла, директории.

Настройками http сервера должнен быть предусмотрен запуск index.php для любых переданных адресов.

Nginx 
```
rewrite ^(.*)$ /index.php last;
```
Apache
```
RewriteEngine on
RewriteRule ^.*$ / [NC,L,QSA]
```

2. В url содержится информация для создания объекта с параметрами по умолчанию или настройками передаваемые после вопросительного знака, для этого используется сопоставление пространства имен приложения.

Ниже приведен пример сопоставления для адреса https://example.ru/test/object

Таблица 1. Пример сопоставления с пространством имен

| Префикс | директория | объект |
https://example.ru/ | test/ | test |
/srv/http/example.ru/ | test/ | test.php |
namespace | test/ | new test(); | 
